def whois_lookup(domain: str):
5 = socket.socket(socket.AF_INET,
socket.SOCK_STREAM)
s. connect(("whois.iana.org", 43))
5. send (f"'(domain) \r\n*
- encode())
response
5. recv (4096) - decode()
s. close()
return
